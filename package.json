{
  "name": "mqtt-influx-bridge",
  "version": "1.0.0",
  "description": "MQTT to InfluxDB bridge for tractor data",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "mqtt": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
